#include<stdio.h>

int main() {
int arr[100000];
    int a,n;
    //scanf("%d",&n);
    for(a=0;a < 100;a++)
    {
    scanf("%d",&arr[a]);
    }
    int count = 0;
    int max = 0;
    for(a=0;a < 100;a++) {
        if(arr[a] == 1)
        {
            count++;
            if(count > max)
            {
                max = count;
            }
        }
        else //if(arr[a] !=1)
        {
            count = 0;
        }
        
    }
    printf("%d\n",max);
    return 0;
}

